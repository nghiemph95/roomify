# Puter Worker URL – bắt buộc cho getProjects, getProject, createProject, updateProject.
#
# Local: copy thành .env.local và điền URL, rồi restart dev server (npm run dev).
# Deploy: set biến môi trường trong dashboard của hosting (Vercel / Netlify / …):
#   - Tên: VITE_PUTER_WORKER_URL
#   - Giá trị: https://your-worker.puter.work/
# Sau đó build lại (build sẽ inject giá trị vào app).
VITE_PUTER_WORKER_URL=https://your-worker.puter.work/
